# OAuth Backend

Node.js and Express backend scaffold for OAuth authentication.

## Setup

```bash
npm install
npm run dev
```

The server starts on `http://localhost:3000` by default.

- Health check: `GET /api/v1/health`

Copy `.env.example` to `.env` and add your local values. OAuth flows are intentionally not implemented yet.
